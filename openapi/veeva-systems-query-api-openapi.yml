openapi: 3.1.0
info:
  title: Veeva Vault Authentication Query API
  version: '26.1'
  description: Minimal OpenAPI for the Veeva Vault Platform REST API covering authentication, session management, version discovery, VQL queries, MDL execution, metadata components, Direct Data files, and delegated sessions.
  x-generated-from: https://developer.veevavault.com/api/26.1/
  x-generated-by: claude-crawl-2026-05-08
servers:
- url: https://{vaultDNS}/api/{version}
  description: Vault REST API
  variables:
    vaultDNS:
      default: myvault.veevavault.com
      description: Customer Vault DNS hostname
    version:
      default: v26.1
      description: API version
security:
- SessionId: []
tags:
- name: Query
paths:
  /query:
    post:
      tags:
      - Query
      summary: Execute a VQL query
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                q:
                  type: string
      responses:
        '200':
          description: Query result
          content:
            application/json:
              schema:
                type: object
components:
  securitySchemes:
    SessionId:
      type: apiKey
      in: header
      name: Authorization
      description: Vault Session ID returned by /auth, sent as the Authorization header.
